extends templates/template
block content
    header.no-banner
        include menu-rojo.pug

    .section-contact-email.container
        .title-contact-email
            h2 Contáctenos
                .hr
                    hr
                    img(src="/img/hr/semaphore.svg" alt="Semáforo")
                    hr
        form(action='/mail' method='POST')
            input.login-form-input(type='text' name='nombres' placeholder='Nombres' id="name" required)
            input.login-form-input(type='text' name='apellidos' placeholder='Apellidos' id="name" required)
            input.login-form-input(type='tel' name='telefono' placeholder='Teléfono' required)
            input.login-form-input(type='text' name='email' placeholder='Email' required)
            textarea.login-form-input(name='mensaje' placeholder='Mensaje' required)
            input.login-form-submit(type='submit' name='submit' value="Enviar")